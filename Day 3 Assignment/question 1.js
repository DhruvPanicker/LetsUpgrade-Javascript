let num = prompt("Type your number here")
if (num % 2 === 0) {
    console.log(`The number entered is ${num} and it is an EVEN number`);
} else {
    console.log(`The number entered is ${num} and it is an ODD number`)
}